name: app_release_center
description: "Desktop release automation center for Flutter projects."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  get: ^4.7.3
  path: ^1.9.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
